name: execute_scraper
on:
  schedule:
    - cron: "40 * * * *"

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Execute the scraper
        run: |
          python /home/ubuntu/src/hello.py
      - name: Check Result
        run: |
          cat output.txt
